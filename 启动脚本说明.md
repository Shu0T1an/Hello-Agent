# Hello-Agent 启动脚本使用说明

## 📁 脚本文件

| 文件 | 说明 |
|------|------|
| `start-all.bat` | 批处理脚本 - 在新窗口启动前后端服务 |
| `start-all.ps1` | PowerShell 脚本 - 在当前窗口启动，统一输出日志 |
| `stop-all.bat` | 停止脚本 - 停止所有服务并清理端口占用 |

## 🚀 使用方法

### 方法一：批处理脚本 (推荐新手)

**启动：**
1. 双击 `start-all.bat`
2. 会自动打开两个新窗口：
   - "Hello-Agent Backend" - 后端日志
   - "Hello-Agent Frontend" - 前端日志
3. 等待服务启动完成后会自动打开浏览器

**停止：**
- 直接关闭两个窗口即可

### 方法二：PowerShell 脚本 (推荐开发者)

**启动：**
1. 右键点击 `start-all.ps1`
2. 选择 "使用 PowerShell 运行"
3. 或者在 PowerShell 中运行：
   ```powershell
   .\start-all.ps1
   ```

**停止：**
- 按 `Ctrl+C` 或关闭窗口

### 方法三：停止脚本

如果端口被占用无法启动，运行：
```bash
stop-all.bat
```

## 📋 前置要求

### 后端要求
- JDK 21+
- Maven 3.6+
- PostgreSQL (如果使用数据库功能)

### 前端要求
- Node.js 18+
- npm 或 pnpm

## 🔍 故障排查

### 端口被占用

如果提示端口被占用，运行：
```bash
# 查看端口占用
netstat -ano | findstr "8080"
netstat -ano | findstr "5173"

# 或直接运行停止脚本
stop-all.bat
```

### PowerShell 执行策略错误

如果出现 "无法加载文件，因为在此系统上禁止运行脚本"：

**临时解决方案（推荐）：**
```powershell
powershell -ExecutionPolicy Bypass -File start-all.ps1
```

**永久解决方案（需要管理员权限）：**
```powershell
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Maven 编译失败

1. 确保 JDK 版本正确：
   ```bash
   java -version
   ```

2. 清理并重新编译：
   ```bash
   cd Agent-Studio
   mvn clean install
   ```

### 前端依赖安装失败

1. 删除 node_modules 并重新安装：
   ```bash
   cd frontend
   rmdir /s node_modules
   npm install
   ```

2. 或使用国内镜像：
   ```bash
   npm config set registry https://registry.npmmirror.com
   ```

## 📝 启动后访问

- 前端界面: http://localhost:5173
- 后端 API: http://localhost:8080
- 健康检查: http://localhost:8080/actuator/health
- API 文档: http://localhost:8080/doc/index.html (如果配置了)

## 🛠️ 开发模式

如果需要单独启动服务进行调试：

**仅启动后端：**
```bash
cd Agent-Studio
mvn spring-boot:run
```

**仅启动前端：**
```bash
cd frontend
npm run dev
```

## 📌 注意事项

1. 首次运行 Maven 会下载依赖，可能需要较长时间
2. 确保 PostgreSQL 已启动（如果使用数据库功能）
3. 建议使用 PowerShell 脚本，可以在同一窗口查看所有日志
4. 批处理脚本会在独立窗口运行，方便分别查看前后端日志

## 🔧 自定义配置

如需修改端口或其他配置，编辑以下文件：

- 后端配置: `Agent-Studio/src/main/resources/application.yml`
- 前端配置: `frontend/vite.config.ts`
- 启动脚本: 根据需要修改 `.bat` 或 `.ps1` 文件
